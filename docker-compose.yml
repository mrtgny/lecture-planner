version: '3'
services:
  nodejs-server:
    build:
      context: ./
    ports:
      - "3001:3000"
    container_name: lecture-schedule
    volumes:
       - ./:/usr/src/app/mrtgny/website
       - /usr/src/app/mrtgny/website/node_modules
